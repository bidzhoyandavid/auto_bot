# Auto Deal Bot

AI-агент для мониторинга выгодных предложений автомобилей на list.am (Армения) и myauto.ge (Грузия).

## Функции

- 🔍 Парсинг list.am и myauto.ge каждые 25 минут
- 🚗 Фильтрация: BMW, Mercedes, Audi, Lexus (2020+, до $20,000)
- 📊 Анализ цен (сравнение с рынком по перцентилям)
- ⚡ Детекция срочных продаж (ключевые слова + падение цены)
- 📱 Уведомления в Telegram о выгодных сделках
- 🔄 Ротация бесплатных прокси

## Установка

### 1. Клонирование и настройка окружения

```bash
cd d:\Startups\auto_bot
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Установка Playwright

```bash
playwright install chromium
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
copy .env.example .env
```

Отредактируйте `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

#### Как получить:

**TELEGRAM_BOT_TOKEN:**

1. Напишите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям, получите токен

**TELEGRAM_CHAT_ID:**

1. Напишите боту @userinfobot
2. Он покажет ваш chat_id

## Запуск

```bash
python -m src.main
```

Или:

```bash
cd src
python main.py
```

## Структура проекта

```
auto_bot/
├── src/
│   ├── main.py              # Точка входа, планировщик
│   ├── config.py            # Конфигурация
│   ├── proxy_manager.py     # Менеджер бесплатных прокси
│   ├── scrapers/
│   │   ├── base.py          # Базовый скрапер (Playwright)
│   │   ├── list_am.py       # Скрапер list.am
│   │   └── myauto_ge.py     # Скрапер myauto.ge
│   ├── analyzers/
│   │   ├── price_analyzer.py    # Анализ цен
│   │   └── urgency_detector.py  # Детекция срочности
│   ├── bot/
│   │   └── telegram_bot.py  # Telegram бот (aiogram)
│   └── database/
│       ├── models.py        # SQLAlchemy модели
│       └── repository.py    # Операции с БД
├── requirements.txt
├── .env.example
├── .gitignore
└── README.md
```

## Конфигурация

Все параметры можно изменить в `.env`:

| Параметр                  | Описание                       | По умолчанию            |
| ------------------------- | ------------------------------ | ----------------------- |
| `SCRAPE_INTERVAL_MINUTES` | Интервал парсинга              | 25                      |
| `REQUEST_DELAY_MIN`       | Мин. задержка между запросами  | 5                       |
| `REQUEST_DELAY_MAX`       | Макс. задержка между запросами | 15                      |
| `TARGET_BRANDS`           | Марки для поиска               | BMW,Mercedes,Audi,Lexus |
| `MIN_YEAR`                | Минимальный год                | 2020                    |
| `MAX_PRICE_USD`           | Максимальная цена              | 20000                   |

## Как работает

1. **Планировщик** запускает задачу каждые 25 минут
2. **Скраперы** (Playwright + stealth) собирают объявления с сайтов
3. **Прокси-менеджер** ротирует бесплатные прокси для обхода блокировок
4. **Анализатор цен** сравнивает цену с рынком (перцентиль P20)
5. **Детектор срочности** ищет ключевые слова и падения цены
6. **Telegram бот** отправляет уведомления о выгодных сделках

## Критерии уведомления

Бот отправит уведомление, если:

- ✅ Новое объявление с ценой ниже 20-го перцентиля рынка
- ✅ Объявление со словами "срочно", "торг", "urgent" и т.д.
- ✅ Цена упала на 5%+ с прошлого сканирования

## Дедупликация

- Объявления хранятся в SQLite с уникальным ключом `(source, listing_id)`
- История цен сохраняется для отслеживания изменений
- Уведомления не дублируются в течение 24 часов

## Логи

Логи пишутся в консоль и файл `auto_bot.log`.

## Troubleshooting

### Бот не отправляет сообщения

- Проверьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
- Убедитесь, что вы написали боту `/start`

### Сайты блокируют запросы

- Увеличьте `REQUEST_DELAY_MIN` и `REQUEST_DELAY_MAX`
- Прокси-менеджер автоматически обновляет пул каждые 15 минут

### Playwright не работает

```bash
playwright install chromium
playwright install-deps
```

## License

MIT
